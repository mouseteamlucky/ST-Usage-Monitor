# 验证报告（实测数据）

> 环境：SillyTavern 1.18.0（release 51ad27fb8）/ Windows / 模型 **deepseek-flash**（官方 API）
> 会话：长篇中文角色扮演，单次 prompt 约 5.5 万 ~ 6.8 万 token，缓存活跃窗口内连续请求

## 1. 采集链路跑通（2026-09-13）

第一次成功的记录（data/<user>/st-usage.jsonl）：

```json
{"t":"2026-09-13T07:35:30.295Z","source":"deepseek","model":"deepseek-flash","n_msgs":6,
 "prompt":67572,"hit":21120,"miss":46452,"cached":21120,"completion":16060}
```

调试轨迹 st-usage.jsonl.debug.log 同步写入：

```text
2026-09-13T07:34:03.300Z [capture] start status=200 model=deepseek-flash n_msgs=6 stream=true
2026-09-13T07:35:30.298Z [capture] end bytes=11672519 parsed=yes hit=21120 miss=46452 prompt=67572
```

## 2. 连续 8 条记录的归因

| 时间(本地) | prompt | hit | miss | 命中率 | 输出 | 归因 |
|---|---|---|---|---|---|---|
| 15:35 | 67,572 | 21,120 | 46,452 | 31% | 16,060 | 普通新回合 → 仅前缀命中（旧顺序 20.7k） |
| 15:43 | 67,547 | **0** | 67,547 | 0% | 5,717 | **命名空间切换**（刚关掉原生推理） |
| 15:49 | 67,885 | 34,944 | 32,941 | 51% | 7,763 | 重 roll → 前缀 + 窗口约 14k |
| 15:52 | 67,885 | 67,712 | 173 | **99.7%** | 6,680 | 同 payload 再滑一次 → 几乎全中 |
| 16:20 | 67,713 | 19,456 | 48,257 | 29% | 5,970 | 普通新回合 → 前缀命中 |
| 17:59 | 55,726 | 25,728 | 29,998 | 46% | 4,878 | 前缀 + 5k 窗口（部分共享） |
| 18:03 | 55,370 | **128** | 55,242 | 0.2% | 5,459 | 又一次命名空间/档位切换 |
| 18:57 | 66,588 | 25,728 | 40,860 | 39% | 10,940 | 前缀 + 5k 窗口 |

**观察**：同一套配置下命中 0 ~ 67,712 全部出现，且都**可解释** —— 说明"命中率抖动"多数不是故障，
而是「请求形态（新回合 / 重 roll）」+「缓存命名空间」两件事混在一起。

hit 全部是 **64 的整数倍**（21,120 = 64×330；34,944 = 64×546；67,712 = 64×1058 …），
与提供方缓存按 64 token 块对齐的行为一致。

## 3. 命中"台阶"读数表

以一次实测的提示词结构为例（预设前缀 25,594 + 世界书 position=0 常驻 11,108 + 角色卡约 700）：

| hit 落点 | 含义 |
|---|---|
| **≈37,400** | 整个前缀全命中 ✅ 正常基准线 |
| ≈34,800 | 世界书里最后一条（自动总结写入的「小总结」）被重写 |
| ≈25,600 | 世界书里更靠前的条目被改写（人设 / 世界总纲等） |
| ≈0 ~ 几百 | 缓存命名空间切换（思考模式开关、模型档位）或缓存过期 |
| >37,400 | 该条是重 roll / 滑切，额外吃到了聊天窗口那一段 |

## 4. 真实花费（用官方新价折算）

按 2026-09-10 起 deepseek-flash 空闲价（命中 ¥0.02/M、未命中 ¥1/M、输出 ¥4/M）：

| 场景 | 算式 | 成本 |
|---|---|---|
| 上表 4 条合计 | hit 123,776 / miss 147,113 / 输出 36,220 | **¥0.2945** |
| 假如完全无缓存 | (123,776+147,113)×¥1/M + 36,220×¥4/M | ¥0.4158 |
| **缓存省下** | — | **¥0.1213（29%）** |

**输出比输入贵**：上表 4 条里输出 36,220 token 贡献 ¥0.1449，占 49%。
实测把模型的**原生推理**（extra.reasoning，单条曾达 31,569 字符 ≈ 8k 输出 token、耗时 247 秒）
关掉后，输出从 16,060 降到 5,717 ~ 7,763 token/条 —— **每条省约 ¥0.037，比任何输入侧优化的收益都大**。

## 5. 提示词顺序实验

同一会话、同一模型，把 53 条**静态**尾部条目从 Chat History 之后移动到之前
（16,691 token 从"每轮全价"变成"每轮命中价"）：

| | 前缀命中 | 单条成本（新价） |
|---|---|---|
| 移动前 | ≈20,700 | ¥0.0904（实测 16:20：prompt 67,713 / hit 19,456） |
| 移动后（预期） | ≈37,400 | 约 ¥0.074（−15%） |

> 顺序调整**不改变模型看到的文字内容**（同一批条目、同样的相对顺序），只改变它们位于
> 缓存分界线的哪一侧 —— 原理见 docs/cache-optimization.zh.md。

## 6. 已知边界

- 只覆盖 deepseek 与 custom 两个 chat completion 源；
- 上游不返回 usage 时无法记录（debug.log 会显示 parsed=NO）；
- stream_options: { include_usage: true } 是拿到流式 usage 的前提，个别中转站可能不支持
  （可用 ST_USAGE_STREAM_OPTIONS=0 关闭，代价是拿不到 usage）。
